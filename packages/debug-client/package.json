{
  "name": "debug-client",
  "version": "1.0.0",
  "scripts": {
    "start": "BABEL_ENV=node nodemon -x 'wait-on tcp:3000 -w 0 && node client.babel.js'"
  },
  "devDependencies": {
    "nodemon": "^1.11.0",
    "wait-on": "^2.0.2"
  }
}
